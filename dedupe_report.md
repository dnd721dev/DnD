# Deduplicate / Dead-Code Report

Generated by: Bug 3 audit (2026-04-24).  
**No files have been deleted yet.** Review and reply "approved" to proceed.

---

## Dead Components — safe to delete

These files are exported but never imported anywhere else in the codebase.
Deleting them removes dead weight without breaking anything.

### 1. `src/components/table/FogOfWarOverlay.tsx`

**Why it's dead:**  
Legacy fog-of-war component that fills a canvas with `rgba(0,0,0,0.75)` and
lets the GM drag-clear rectangles. No file in the project imports it.

**Superseded by:**  
The tile-based fog system inside `MapBoardView.tsx` (persistent DB-backed
`fog_reveals` table, circle reveals, destination-out compositing). The GM brush
is handled by `MapBoard.tsx`'s built-in `fogToolActive` mode.

**Risk:** None — the component is completely unreferenced.

---

### 2. `src/components/character-sheet/InventoryPanel.tsx`

**Why it's dead:**  
An older inventory UI that stored items with `kind` (weapon/armor/shield/misc)
and offered simple add/remove controls. No file imports `InventoryPanel`.

**Superseded by:**  
`EquipmentPanel.tsx`, which is a full superset: it manages the inventory,
equipped weapon/armor/shield, attunement (max 3 items), homebrew items, and
auto-saves on every change. It is actively imported in `characters/[id]/page.tsx`.

**Risk:** None — the component is completely unreferenced.

---

### 3. `src/components/table/TableHeader.tsx`

**Why it's dead:**  
Renders a session/campaign header (title, status, "Back to lobby" link, GM
badge). No file imports it.

**Superseded by:**  
`src/components/table/tableclient/components/TableTopBar.tsx`, which is the
active session header rendered inside `TableClient.tsx`.

**Risk:** None — the component is completely unreferenced.

---

## Duplicate Logic — keep both files, consolidate call site

### 4. `src/lib/equipmentRules.ts` not wired into `EquipmentPanel.tsx` (Bug 6)

**Current state:**  
`equipmentRules.ts` exports `classCanUseWeapon()` and `classCanUseArmor()` — a
complete class-proficiency filter already written and correct.  
`EquipmentPanel.tsx` builds its weapon/armor dropdowns from the full `WEAPONS` /
`ARMORS` tables with **no filtering** — a Fighter and a Wizard both see every
weapon and every armor.

**Action required (Bug 6):**  
Wire `classCanUseWeapon` / `classCanUseArmor` into `EquipmentPanel`'s
`buildLibOptions()` function so only class-appropriate items appear in the
"Add item" dropdown. This is a code-addition, not a deletion.

---

## Summary

| # | File | Action | Risk |
|---|------|--------|------|
| 1 | `src/components/table/FogOfWarOverlay.tsx` | Delete | None |
| 2 | `src/components/character-sheet/InventoryPanel.tsx` | Delete | None |
| 3 | `src/components/table/TableHeader.tsx` | Delete | None |
| 4 | `src/lib/equipmentRules.ts` usage | Wire into EquipmentPanel (Bug 6) | Low |

**Awaiting approval before any deletions.**
